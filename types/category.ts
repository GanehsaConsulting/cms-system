export interface CustomArticleCategory {
  id: string;
  brandId: string;
  label: string;
  badgeClassName: string;
  createdAt: string;
}

export interface CustomArticleCategoryInput {
  label: string;
}
